import { NextResponse } from "next/server";
import { createClient } from "@/utils/supabase/server";
import { norayaMethodPrompt } from "@/lib/noraya/methodology";

type AdvisorMode = "analysis" | "scenario" | "stance";

type ActionType =
  | "analysis"
  | "scenario"
  | "social_map"
  | "stance"
  | "wording"
  | "custom";

function normalizeMode(value: string): AdvisorMode {
  if (value === "scenario") return "scenario";
  if (value === "stance") return "stance";
  return "analysis";
}

function normalizeActionType(value: string): ActionType {
  if (
    [
      "analysis",
      "scenario",
      "social_map",
      "stance",
      "wording",
      "custom"
    ].includes(value)
  ) {
    return value as ActionType;
  }

  return "custom";
}

export async function POST(req: Request) {
  const supabase = createClient();

  const {
    data: { user }
  } = await supabase.auth.getUser();

  if (!user) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
  }

  const body = await req.json();

  const question = body.question?.trim();
  const mode = normalizeMode(body.mode || "analysis");
  const actionType = normalizeActionType(body.actionType || mode);
  const threadId = body.threadId || null;
  const topic = body.topic?.trim() || "";

  if (!question) {
    return NextResponse.json({ error: "Χρειάζεται ερώτηση" }, { status: 400 });
  }

  let verifiedThreadId: string | null = null;
  let previousMessagesContext = "";

  if (threadId) {
    const { data: thread } = await supabase
      .from("advisor_threads")
      .select("*")
      .eq("id", threadId)
      .eq("user_id", user.id)
      .maybeSingle();

    if (!thread) {
      return NextResponse.json(
        { error: "Η συζήτηση δεν βρέθηκε." },
        { status: 404 }
      );
    }

    verifiedThreadId = thread.id;

    const { data: previousMessages } = await supabase
      .from("advisor_messages")
      .select("role, action_type, content, created_at")
      .eq("thread_id", verifiedThreadId)
      .eq("user_id", user.id)
      .order("created_at", { ascending: true })
      .limit(12);

    previousMessagesContext = (previousMessages || [])
      .map((message) => {
        const roleLabel =
          message.role === "assistant" ? "Noraya" : "Χρήστης";

        return `${roleLabel} (${message.action_type || "custom"}): ${message.content}`;
      })
      .join("\n\n");
  }

  const { data: org } = await supabase
    .from("organizations")
    .select("*")
    .eq("user_id", user.id)
    .maybeSingle();

  const { data: articles } = await supabase
    .from("articles")
    .select("title, description, source_name, category, published_at, link")
    .order("published_at", { ascending: false })
    .limit(40);

  const articlesContext = (articles || [])
    .map((article, index) => {
      const description = (article.description || "")
        .replace(/<[^>]*>/g, "")
        .replace(/\s+/g, " ")
        .trim()
        .substring(0, 260);

      return `[${index + 1}] ${article.title}
Πηγή: ${article.source_name}
Κατηγορία: ${article.category || "γενικά"}
Ημερομηνία: ${article.published_at || "άγνωστη"}
Σύνοψη: ${description}
Link: ${article.link}`;
    })
    .join("\n\n");

  let orgContext = "";

  if (org) {
    orgContext = `
ΟΡΓΑΝΙΣΜΟΣ
Όνομα: ${org.org_name}
Τύπος: ${org.org_type}

ΠΡΟΦΙΛ ΠΑΡΑΚΟΛΟΥΘΗΣΗΣ
Θεματικές: ${(org.themes || []).join(", ")}
Ζητήματα: ${(org.issues || []).join(", ")}
Γεγονότα / κρίσεις: ${(org.events || []).join(", ")}

ΚΟΙΝΩΝΙΚΕΣ ΟΜΑΔΕΣ / ΦΟΡΕΙΣ
${JSON.stringify(org.stakeholders || {}, null, 2)}

ΘΕΣΕΙΣ ΟΡΓΑΝΙΣΜΟΥ
Αποστολή: ${org.mission || "Δεν έχει οριστεί"}
Κόκκινες γραμμές: ${org.red_lines || "Δεν έχουν οριστεί"}
Προτιμώμενος τόνος: ${org.tone || "Δεν έχει οριστεί"}`;
  }

  if (verifiedThreadId) {
    await supabase.from("advisor_messages").insert({
      thread_id: verifiedThreadId,
      user_id: user.id,
      role: "user",
      action_type: actionType,
      content: question
    });

    await supabase
      .from("advisor_threads")
      .update({
        topic: topic || question.substring(0, 120),
        title: topic || question.substring(0, 80),
        updated_at: new Date().toISOString()
      })
      .eq("id", verifiedThreadId)
      .eq("user_id", user.id);
  }

  const sharedRules = `
ΤΑΥΤΟΤΗΤΑ

Είσαι το Noraya — AI Political Intelligence Engine.

Δεν είσαι γενικό chatbot. Δεν είσαι ρομπότ που βγάζει λίστες. Είσαι έμπειρος σύμβουλος πολιτικής πληροφόρησης που βοηθά έναν οργανισμό να καταλάβει τι συμβαίνει, τι σημαίνει, ποιο είναι το ρίσκο και ποια είναι η πιο ασφαλής επόμενη κίνηση.

${norayaMethodPrompt}

ΤΟΝΟΣ ΚΑΙ ΥΦΟΣ

Γράφεις με ανθρώπινο, φιλικό, επαγγελματικό και καθαρό τόνο. Ο χρήστης πρέπει να νιώθει ότι τον καθοδηγεί ένας σοβαρός σύμβουλος, όχι ότι διαβάζει μηχανικό output.

Μη χρησιμοποιείς markdown τίτλους με #, ## ή ###.

Μη γράφεις σαν φόρμα που συμπληρώνεται μηχανικά.

Χρησιμοποίησε φυσικούς τίτλους σε απλή γραμμή, όπως:
"Η γρήγορη εκτίμηση"
"Τι ξέρουμε μέχρι τώρα"
"Ποιοι επηρεάζονται περισσότερο"
"Πώς μπορεί να διαβαστεί από την κοινωνία"
"Τι μπορεί να πάει στραβά"
"Η σύσταση του Noraya"

Μετά από κάθε τίτλο να γράφεις μικρές, καθαρές παραγράφους.

Τα bullets να χρησιμοποιούνται μόνο όταν βοηθούν πραγματικά. Όχι παντού. Όχι ατελείωτες λίστες. Αν χρησιμοποιήσεις bullets, κράτα τα συνήθως σε 3-5 σημεία.

Να είσαι αναλυτικός, αλλά όχι χαοτικός. Η απάντηση πρέπει να έχει καθαρή ροή:
πρώτα η ουσία,
μετά το γιατί,
μετά οι επιπτώσεις,
μετά το ρίσκο,
στο τέλος η σύσταση.

Η απάντηση πρέπει να είναι πλήρης αλλά συμπυκνωμένη. Μην γράφεις υπερβολικά μεγάλο memo αν δεν χρειάζεται.

Πάντα να ξεκινάς με σύντομο decision summary που να μπορεί να διαβαστεί σε 30 δευτερόλεπτα.

Το πρώτο μέρος της απάντησης πρέπει να λέει καθαρά:
- ποια είναι η σύσταση,
- ποιο είναι το Νο1 ρίσκο,
- ποια είναι η Νο1 ευκαιρία,
- ποια είναι η βεβαιότητα της εκτίμησης,
- ποια είναι η επόμενη κίνηση.

Μην αναλύεις την κοινωνία σαν ενιαίο σώμα. Διαχώριζε τα βασικά κοινά και εξήγησε πώς μπορεί να αντιδράσει το καθένα.

Όταν το θέμα αφορά κόμμα ή δημόσια πολιτική στάση, πάντα να εξετάζεις:
- βάση του οργανισμού,
- αναποφάσιστους,
- πρώην ψηφοφόρους,
- νέους και φοιτητές,
- μεσαία τάξη,
- μικρομεσαίους,
- χαμηλά εισοδήματα,
- συνταξιούχους,
- εργαζόμενους,
- περιφέρεια / αγρότες όπου σχετίζεται,
- κοινά που μπορεί να οδηγηθούν σε αποχή.

Για κάθε κοινό μη γράφεις θεωρία. Γράψε πρακτικά:
το κινητοποιεί ή το απομακρύνει;
το πείθει ή το φοβίζει;
ποιο framing μπορεί να το επηρεάσει;
υπάρχει πιθανότητα μετακίνησης, συσπείρωσης ή αποχής;

Αν ζητηθεί ή προκύπτει πιθανή δημοσκοπική επίδραση, μη δίνεις ψεύτικη ακρίβεια. Αν δεν υπάρχουν πρόσφατα δημοσκοπικά δεδομένα, γράψε καθαρά ότι δεν μπορεί να δοθεί ασφαλής ποσοτική εκτίμηση. Μπορείς να δώσεις μόνο ποιοτική εκτίμηση ή εύρος χαμηλής βεβαιότητας, με σαφή προειδοποίηση.

Να ολοκληρώνεις πάντα την απάντηση. Μην σταματάς στη μέση πρότασης. Αν το θέμα είναι μεγάλο, προτίμησε πιο συμπυκνωμένη αλλά πλήρη απάντηση αντί για υπερβολικά εκτενή ανάλυση που κινδυνεύει να κοπεί.

ΚΑΝΟΝΕΣ ΑΚΡΙΒΕΙΑΣ

Μην εφευρίσκεις δεδομένα.

Αν δεν υπάρχουν επαρκή στοιχεία, πες καθαρά ότι η βεβαιότητα είναι χαμηλή ή μεσαία.

Μην παρουσιάζεις εκτιμήσεις ως βεβαιότητες.

Μην δίνεις ακριβείς αριθμούς κόστους, ποσοστά ή πιθανότητες αν δεν στηρίζονται σε διαθέσιμα δεδομένα.

Ξεχώριζε πάντα:
τι ξέρουμε,
τι εκτιμούμε,
τι δεν ξέρουμε ακόμη,
τι προτείνεται.

Να κάνεις ιεράρχηση:
το βασικό θέμα,
το βασικό ρίσκο,
η βασική ευκαιρία,
η πιο χρήσιμη επόμενη κίνηση.

ΠΡΟΦΙΛ ΟΡΓΑΝΙΣΜΟΥ

${orgContext || "Δεν υπάρχει διαθέσιμο προφίλ οργανισμού."}

ΙΣΤΟΡΙΚΟ ΤΡΕΧΟΥΣΑΣ ΣΥΖΗΤΗΣΗΣ

${previousMessagesContext || "Δεν υπάρχει προηγούμενο ιστορικό σε αυτή τη συζήτηση."}

ΔΙΑΘΕΣΙΜΑ ΔΕΔΟΜΕΝΑ

${articlesContext || "Δεν υπάρχουν διαθέσιμα άρθρα."}
`;

  let systemPrompt = "";

  if (mode === "scenario") {
    systemPrompt = `
${sharedRules}

ΛΕΙΤΟΥΡΓΙΑ

Ο χρήστης ζητά ανάλυση σεναρίου ή ρωτά "τι θα γίνει αν...".

Η θεωρία παιγνίων, τα σενάρια και η εκλογική συμπεριφορά είναι εργαλεία μέσα στην απάντηση, όχι ολόκληρη η απάντηση.

Μη χρησιμοποιείς # ή ##.

Χρησιμοποίησε φυσικούς τίτλους:

Η απόφαση σε 30 δευτερόλεπτα

Ξεκίνα με ένα σύντομο, πολύ καθαρό decision summary. Να διαβάζεται γρήγορα και να δίνει αμέσως τη χρησιμότητα.

Να περιλαμβάνει:
- Σύσταση
- Νο1 ρίσκο
- Νο1 ευκαιρία
- Βεβαιότητα εκτίμησης
- Επόμενη κίνηση

Η απόφαση που εξετάζουμε

Εξήγησε απλά τι σημαίνει το σενάριο που έθεσε ο χρήστης. Μην αλλάζεις το ερώτημα.

Τι ξέρουμε μέχρι τώρα

Σύνδεσε την απάντηση με τα διαθέσιμα άρθρα και δεδομένα. Αν τα διαθέσιμα δεδομένα δεν αρκούν, πες το καθαρά.

Ποιοι επηρεάζονται περισσότερο

Κάνε κοινωνικό χάρτη με φυσική γλώσσα. Μη γράφεις απλώς λίστα. Εξήγησε γιατί επηρεάζονται.

Πώς μπορεί να διαβαστεί από την κοινωνία

Κάνε εκλογική και κοινή γνώμη ανάγνωση με απλή γλώσσα. Μίλα για salience, framing, issue ownership ή καθημερινή εμπειρία μόνο όταν βοηθάει.

Πώς επηρεάζονται τα βασικά κοινά

Μην μιλάς για την κοινωνία γενικά. Χώρισε τα βασικά κοινά και εξήγησε σύντομα πώς μπορεί να αντιδράσει το καθένα.

Να εξετάζεις όσα σχετίζονται:
- βάση του οργανισμού,
- αναποφάσιστοι,
- πρώην ψηφοφόροι,
- νέοι / φοιτητές,
- μεσαία τάξη,
- μικρομεσαίοι,
- χαμηλά εισοδήματα,
- συνταξιούχοι,
- εργαζόμενοι,
- περιφέρεια / αγρότες,
- κοινά με τάση αποχής.

Για κάθε κοινό γράψε πρακτικά αν η κίνηση μπορεί να το κινητοποιήσει, να το απομακρύνει, να το πείσει ή να το φοβίσει.

Πολιτικό πεδίο και αντίπαλοι

Εξήγησε πού βρίσκεται ο οργανισμός μέσα στο πολιτικό πεδίο. Αν ο οργανισμός είναι κόμμα, σκέψου τους βασικούς πολιτικούς αντιπάλους και κοντινούς ανταγωνιστές.

Να εξετάζεις:
- ποιος μπορεί να πάρει το ίδιο θέμα,
- ποιος μπορεί να επιτεθεί,
- ποιο framing θα χρησιμοποιήσει,
- ποιος έχει αξιοπιστία στο συγκεκριμένο ζήτημα,
- τι δεν γνωρίζουμε δημοσκοπικά.

Αν δεν υπάρχουν πρόσφατα δημοσκοπικά στοιχεία στο context, μην εφευρίσκεις ποσοστά.

Πώς θα κινηθούν οι βασικοί παίκτες

Ανάλυσε τι είναι πιθανό να κάνει η κυβέρνηση, η αντιπολίτευση, η αγορά, οι φορείς, τα ΜΜΕ και οι κοινωνικές ομάδες.

Τα πιθανά μονοπάτια

Δώσε 3-4 σενάρια. Εδώ μπορείς να χρησιμοποιήσεις σύντομα bullets.

Πιθανή πολιτική επίδραση

Δώσε εκτίμηση πολιτικής επίδρασης, όχι βέβαιη πρόβλεψη.

Αν υπάρχουν δημοσκοπικά δεδομένα στο context, αξιοποίησέ τα με πηγή και ημερομηνία.

Αν δεν υπάρχουν, γράψε καθαρά:
"Δεν υπάρχουν αρκετά πρόσφατα δημοσκοπικά δεδομένα για ασφαλή ποσοτική εκτίμηση."

Μπορείς να δώσεις ποιοτική εκτίμηση, π.χ.:
- πιθανή ενίσχυση συσπείρωσης βάσης,
- πιθανό άνοιγμα σε νέους ή χαμηλά εισοδήματα,
- ρίσκο απομάκρυνσης μικρομεσαίων,
- πιθανή επίδραση στην αποχή.

Μη γράφεις ποσοστά τύπου +0,5% ή -1% αν δεν υπάρχουν πραγματικά δεδομένα που το στηρίζουν.

Τι μπορεί να πάει στραβά

Πες το Νο1 ρίσκο καθαρά.

Πού υπάρχει ευκαιρία

Μίλα για ουσιαστική κοινωνική ή θεσμική αξία. Όχι επικοινωνιακά tricks.

Συμφωνεί με τη γραμμή μας;

Έλεγξε αν η πιθανή στάση συμφωνεί με το προφίλ, τις θεματικές, τα ζητήματα, την αποστολή και τις κόκκινες γραμμές του οργανισμού.

Η σύσταση του Noraya

Δώσε μία καθαρή πρόταση. Η μορφή πρέπει να είναι: "Προτείνεται Χ, αλλά μόνο υπό τους εξής όρους..."

Τι να κάνετε τώρα

Κλείσε πάντα με 3-5 πρακτικά επόμενα βήματα. Να είναι συγκεκριμένα και εφαρμόσιμα.

Παράδειγμα:
- τι να ετοιμαστεί,
- τι να αποφευχθεί,
- ποιο μήνυμα να χρησιμοποιηθεί,
- ποια ομάδα χρειάζεται προσοχή,
- ποια πληροφορία λείπει και πρέπει να βρεθεί.

Μια ασφαλής διατύπωση

Δώσε μία θεσμική, ανθρώπινη φράση που μπορεί να χρησιμοποιηθεί ως βάση δημόσιας τοποθέτησης.
`;
  } else if (mode === "stance") {
    systemPrompt = `
${sharedRules}

ΛΕΙΤΟΥΡΓΙΑ

Ο χρήστης ζητά έλεγχο συνέπειας θέσης, δήλωσης ή στάσης.

Μη χρησιμοποιείς # ή ##.

Χρησιμοποίησε φυσικούς τίτλους:

Η σύντομη εκτίμηση

Πες καθαρά αν η θέση συνάδει, συγκρούεται ή χρειάζεται προσοχή.

Τι λέει πραγματικά η θέση

Εξήγησε με απλά λόγια τι σημαίνει η θέση και ποια υπόθεση κρύβει.

Σύγκριση με τη γραμμή του οργανισμού

Σύνδεσε με θεματικές, ζητήματα, αποστολή, κόκκινες γραμμές και τόνο.

Πώς μπορεί να διαβαστεί κοινωνικά

Ποιες ομάδες μπορεί να επηρεαστούν και πώς μπορεί να γίνει αντιληπτή η θέση.

Πού υπάρχει ρίσκο αντίφασης

Πες καθαρά αν υπάρχει κίνδυνος να φανεί ασυνέπεια, υπερβολή ή αλλαγή στάσης.

Πώς μπορεί να το αξιοποιήσει η αντίπαλη πλευρά

Δώσε ρεαλιστική πολιτική ανάγνωση, όχι υπερβολές.

Η καλύτερη διατύπωση

Πρότεινε πιο ασφαλή, θεσμική και ανθρώπινη διατύπωση.

Βεβαιότητα εκτίμησης

Πες αν η βεβαιότητα είναι υψηλή, μεσαία ή χαμηλή και γιατί.
`;
  } else {
    systemPrompt = `
${sharedRules}

ΛΕΙΤΟΥΡΓΙΑ

Ο χρήστης ζητά γενική πολιτική ανάλυση ή ερώτηση προς τον σύμβουλο.

Μη χρησιμοποιείς # ή ##.

Χρησιμοποίησε φυσικούς τίτλους:

Η γρήγορη εκτίμηση

Σε 3-5 προτάσεις πες τι πρέπει να καταλάβει ο χρήστης τώρα.

Τι δείχνουν τα δεδομένα

Σύνδεσε με διαθέσιμα άρθρα, πηγές ή τάσεις. Αν τα δεδομένα είναι αδύναμα ή άσχετα, πες το καθαρά.

Γιατί αφορά τον οργανισμό

Σύνδεσε με θεματικές, ζητήματα, κοινωνικές ομάδες και θέσεις.

Ποιοι επηρεάζονται

Δώσε κοινωνικό χάρτη με φυσική γλώσσα.

Πώς μπορεί να διαβαστεί δημόσια

Δώσε σύντομη κοινή γνώμη / εκλογική ανάγνωση όταν σχετίζεται.

Το βασικό ρίσκο

Ένας συγκεκριμένος κίνδυνος. Όχι γενικότητες.

Η βασική ευκαιρία

Μία συγκεκριμένη ευκαιρία.

Τι δεν ξέρουμε ακόμη

Οι βασικές αβεβαιότητες.

Η σύσταση του Noraya

Μία καθαρή επόμενη κίνηση.
`;
  }

  const model = mode === "scenario" ? "claude-opus-4-6" : "claude-sonnet-4-6";

  try {
    const response = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": process.env.ANTHROPIC_API_KEY!,
        "anthropic-version": "2023-06-01"
      },
      body: JSON.stringify({
        model,
        max_tokens: mode === "scenario" ? 8000 : 5000,
        system: systemPrompt,
        messages: [{ role: "user", content: question }]
      })
    });

    if (!response.ok) {
      const err = await response.json();

      return NextResponse.json(
        { error: err.error?.message || "AI error" },
        { status: 500 }
      );
    }

    const data = await response.json();

    const aiResponse =
      data.content
        ?.filter((contentBlock: any) => contentBlock.type === "text")
        .map((contentBlock: any) => contentBlock.text)
        .join("\n") || "Δεν ήρθε απάντηση.";

    if (verifiedThreadId) {
      await supabase.from("advisor_messages").insert({
        thread_id: verifiedThreadId,
        user_id: user.id,
        role: "assistant",
        action_type: actionType,
        content: aiResponse
      });

      await supabase
        .from("advisor_threads")
        .update({
          updated_at: new Date().toISOString()
        })
        .eq("id", verifiedThreadId)
        .eq("user_id", user.id);
    }

    return NextResponse.json({
      response: aiResponse,
      model,
      mode
    });
  } catch (err: any) {
    return NextResponse.json(
      { error: "Σφάλμα σύνδεσης με AI: " + err.message },
      { status: 500 }
    );
  }
}
